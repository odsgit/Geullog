# Cloudflare Pages Functions에서만 사용 (서버 사이드 전용, 브라우저에 노출되지 않음)
OPENAI_API_KEY=

# 클라이언트에서도 사용 (import.meta.env로 접근, vite.config.ts의 envPrefix 참고)
SUPABASE_URL=
SUPABASE_ANON_KEY=
GA_MEASUREMENT_ID=
CLARITY_PROJECT_ID=
